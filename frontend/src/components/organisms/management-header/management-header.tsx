import React from "react"
import styles from './management-header.module.scss'

type Props = {}

const Component: React.VFC<Props> = ({}) => {
  return (
    <header>
      <h1>Management Header</h1>
    </header>
  )
}

export default Component
